/*!===================================================================
  TYPOGRAPHY
=====================================================================*/

/*! Fonts
=====================================================================*/


/* Vars
=====================================================================*/
$sansSerif = system-ui, -apple-system, 'Segoe UI', Roboto, Ubuntu, Cantarell, 'Noto Sans', 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol', 'Noto Color Emoji', sans-serif
$monospace = 'SFMono-Regular', Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace
$serif = Constantia, 'Lucida Bright', Lucidabright, 'Lucida Serif', Lucida, 'DejaVu Serif', 'Bitstream Vera Serif', 'Liberation Serif', Georgia, serif


$defaultFont = $sansSerif
$fontSize = 16px
$lineHeight = 1.5

$indent = 16px
$indent--xxl = $indent * 4 // 64
$indent--xl = $indent * 3 // 48
$indent--lg = $indent * 2 // 32
$indent--md = $indent / 2 + $indent // 24
$indent--sm = $indent / 2 // 8
$indent--xs = $indent--sm / 2 // 4

$fontSize--xxl = 2.5rem // 48
$fontSize--xl = 2.5rem // 40
$fontSize--lg = 2rem // 32
$fontSize--md = 1.25rem // 20
$fontSize--sm = .875rem // 14
$fontSize--xs = .75rem // 12
$fontSize--xxs = .625rem // 10

$fontSize--h1 = 2.25rem // 36
$fontSize--h2 = 1.875rem // 30
$fontSize--h3 = 1.5rem // 24
$fontSize--h4 = 1.375rem // 22
$fontSize--h5 = 1.25rem // 20
$fontSize--h6 = 1.125rem // 18


/* Mixins
=====================================================================*/
/*! Fluid Text with fallback
* 1rem - font size
* .25 (4px) - max font size => 16px+4px=20px
* 20rem (320px) - min width
* 80 (1280px) - max width.
*/

fluidTxtInRem($fz = 1rem, $step = .25) // .25=4px, .125-2px, .0625=1px
  font-size: $fz
  font-size: 'calc(%s + %s * ((100vw - 20rem) / 80))' % ($fz $step)

fluidTxtInPx($fz = $fontSize, $step = 4)
  font-size: $fz
  font-size: 'calc(%s + %s * ((100vw - 20rem) / 80))' % ($fz $step)

truncated($width = 50%)
  overflow: hidden
  max-width: $width
  white-space: nowrap
  text-overflow: ellipsis


/*! Rules for text elements
=====================================================================*/
h1, h2, h3, h4, h5, h6
  margin-top: 0
  margin-bottom: $indent--sm
  line-height: 1.25

h1
  fluidTxtInRem($fontSize--h1, .5) // 36
h2
  fluidTxtInRem($fontSize--h2, .5) // 30
h3
  fluidTxtInRem($fontSize--h3, .5) // 24
h4
  fluidTxtInRem($fontSize--h4, .5) // 22
h5
  fluidTxtInRem($fontSize--h5, .5) // 20
h6
  fluidTxtInRem($fontSize--h6, .5) // 18

p
  margin-top: 0
  margin-bottom: $indent
  fluidTxtInRem(, .125) // 18

address
  margin-bottom: $indent
  font-style: normal
  line-height: inherit

dl
  margin-top: 0
  margin-bottom: $indent

dt
  font-weight: bold

b, strong
  font-weight: bolder

small
  font-size: 80%

pre, code, kbd, samp
  font-family: $monospace
  font-size: inherit
